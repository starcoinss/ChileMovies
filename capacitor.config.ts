import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.chilemovies',
  appName: ' ChileMovies app android',
  webDir: 'www'
  
};

export default config;
